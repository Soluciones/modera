development:
  adapter: postgresql
  database: modera_engine
  host: localhost
  username: root
  password:
  encoding: utf8
  reconnect: true

# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: modera_engine_test
  host: localhost
  username: root
  password:
  encoding: utf8
  reconnect: true
